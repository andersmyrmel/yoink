{
  "name": "yoink-design-system-extractor",
  "version": "1.0.0",
  "description": "Chrome extension to extract and analyze design system tokens from any website",
  "scripts": {
    "build": "npm run compile && npm run bundle && npm run copy-assets",
    "compile": "tsc",
    "bundle": "npm run bundle-content && npm run bundle-background && npm run bundle-popup",
    "bundle-content": "esbuild dist/scripts/contentScript.js --bundle --outfile=dist/scripts/contentScript.js --format=iife --platform=browser --allow-overwrite",
    "bundle-background": "esbuild dist/scripts/background.js --bundle --outfile=dist/scripts/background.js --format=esm --platform=browser --allow-overwrite",
    "bundle-popup": "esbuild dist/scripts/popup.js --bundle --outfile=dist/scripts/popup.js --format=iife --platform=browser --allow-overwrite",
    "watch": "tsc --watch",
    "copy-assets": "npm run copy-html && npm run copy-css && npm run copy-icons && npm run copy-manifest",
    "copy-html": "cp src/popup.html dist/",
    "copy-css": "cp src/styles/popup.css dist/",
    "copy-icons": "cp -r icons dist/",
    "copy-manifest": "cp manifest.json dist/",
    "clean": "rm -rf dist && mkdir dist",
    "dev": "npm run clean && npm run build && npm run watch",
    "package": "npm run clean && npm run build && cd dist && zip -r ../yoink-extension.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.26.0",
    "typescript": "^5.3.3"
  },
  "keywords": [
    "chrome-extension",
    "design-system",
    "css-variables",
    "design-tokens",
    "developer-tools"
  ],
  "author": "Anders",
  "license": "MIT"
}
